Crie uma task padronizada no Jira para o projeto GymFlow.

Contexto do pedido: $ARGUMENTS

Use o agente `create-jira-task` para:
1. Interpretar o pedido acima e inferir todos os detalhes necessários
2. Montar a descrição completa no padrão do projeto (Objetivo, seções específicas ao tipo, Critérios de Aceite com checkboxes)
3. Criar a issue no Jira (projeto GYM, cloud ID `77128cff-eae2-4af6-aeae-5aae6be3355a`)
4. Retornar a chave (ex: GYM-41) e a URL da issue criada

Se o pedido for vago, complete com base no contexto do GymFlow: React Native + Expo, Clean Architecture, styled-components, TypeScript strict.
