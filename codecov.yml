coverage:
  status:
    patch:
      default:
        target: 70%
        threshold: 5%
    project:
      default:
        target: 70%
        threshold: 5%

comment:
  layout: reach,diff,flags,files
  behavior: default
  require_changes: true
