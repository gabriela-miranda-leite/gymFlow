// TODO: criar os styled-components da tela de cadastro
// Seguir o mesmo padrão de LoginScreen.styles.ts
// Usar apenas tokens de @/tokens (Spacing, etc.) — sem valores hardcoded
// Exemplo: import styled from 'styled-components/native'
